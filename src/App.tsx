import { Button } from "@chakra-ui/react";

const App = () => {
  return (
    <div>
      <Button name="Button" colorScheme="blue">
        Button
      </Button>
    </div>
  );
};

export default App;
